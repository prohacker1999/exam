#include<stdio.h>
#include<string.h>
#include<stdio.h>
{
	int main(int x,int y)
	if(x>y)
	return x;
	else 
	return y;
{
	int main()
	{
	int drop=0,cap,i,process,np[25],count,nsec;
	printf("enter the bucket size:\n");
scanf("%d",&cap);
printf("enter the elements of bucket\n");
scanf("%d",&process);
printf("enter the no of sec to excute\n");
scanf("%d",&nsec);
for(i=0;i<nsec;i++)
{
printf("enter the no of time it excuted at n time",i+1);
scanf("%d",np[i]);
{
printf("\n second | path |accepted |rejected |requsted\n");
printf("-----------------------");
for(i=0;i<nsec;i++)
}
if(count>drop)
{
count=np[i];
mini=min(process,count);
printf("\t%d",process);
printf("\t%d",count);
}
count=count-drop;
printf("\t\t%d",count);
printf("\t\t%d",drop);
drop=0;
}
for( ;count!=0;i++);
count=count-drop;
{
printf("\t0");
mini=min(count,drop);
printf("\t%d",mini);
count=np-count;
printf("\t%d",count);
printf("\t%d",drop);
}
}
